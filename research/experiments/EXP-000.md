# EXP-000 — REF harness smoke (released LIBERO-Spatial ckpt)

- **Date:** 2026-09-20
- **Hypothesis:** Harness check — released `openvla-7b-finetuned-libero-spatial` should succeed far above chance on `libero_spatial` with our office stack (GPU-1, SDPA, MuJoCo 3.3.2). Formal paper gate (~84.7%) still requires full 50 trials/task.
- **Dataset / eval set:** `libero_spatial`, **3 trials/task** (smoke; not frozen final protocol), `--center_crop True`, `unnorm_key` resolved to `libero_spatial` (from HF `dataset_statistics.json`; key `libero_spatial_no_noops` not present in that file).
- **Model / arm:** REF — `openvla/openvla-7b-finetuned-libero-spatial` (HF Hub)
- **Hyperparameters:** n/a (eval only); attn=`sdpa`; bf16 load
- **Hardware:** `vm-130-131` physical **GPU-1** (`CUDA_VISIBLE_DEVICES=1`) RTX PRO 6000 Blackwell 96GB | driver 580.178.04 | torch 2.11.0+cu128 (CUDA 12.8, cap 12,0) | transformers 4.40.1 | bitsandbytes 0.50.2 | mujoco **3.3.2** | robosuite 1.4.1
- **Result (smoke):**
  - **Success: 90.0%** (27 / 30 episodes)
  - Latency / peak mem / size / throughput: not measured this run (metrics.py next)
- **Raw logs:**
  - `research/experiments/ref_smoke_20260920_120809.log`
  - `~/vla/openvla/experiments/logs/EVAL-libero_spatial-openvla-2026_09_20-12_08_25.txt`
- **What failed / surprised me:**
  - Blind `pip install -e openvla` pulls torch 2.2 — always `--no-deps` + keep cu128.
  - `protobuf` / `tensorflow-metadata` clash → settled on protobuf **6.31.1** (TF warns; dlimp/tfds import OK for eval).
  - PyTorch 2.6+ `torch.load` defaults `weights_only=True` → patched LIBERO init-state loads to `weights_only=False`.
  - MuJoCo **3.13** breaks robosuite 1.4.1 (`get_joint_qpos_addr` AssertionError) → pin **mujoco==3.3.2**.
  - HF hub ckpt needs Hub fetch of `dataset_statistics.json` (patched in `openvla_utils.py`).
  - EGL destructor noise on env close (harmless after successful rollouts).
  - Smoke 90% > published ~84.7% — expected noise at n=3; still validates harness ≫ 0%.
- **Next experiment:**
  - Full REF: `--num_trials_per_task 50` (+ seeds) → confirm ~84.7% gate; log five metrics; freeze `research/datasets/libero-spatial-eval.md`.
  - Then Arm A LoRA (`scripts/train_arm_a.sh`).

# Octo — speak-from notes (stub)

- **Why read:** VLA method context and a baseline that appears in LIBERO comparison tables. Lighter-weight transformer policy (diffusion action head) trained on Open X-Embodiment. Useful contrast: Octo is not LLM-backed, so it sits at a different point on the size/capability curve than OpenVLA.
- **Fill on office box / from paper:** architecture (transformer + diffusion head), param count, training data, LIBERO numbers as reported in the OpenVLA / OFT tables.
- Cite + arXiv id: TODO.

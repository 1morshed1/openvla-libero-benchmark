# EXP-001 — Arm A: own LoRA r=32 fine-tune (merged bf16)

- **Date:** 2026-09-21 (launched) · _____ (eval completed)
- **Status:** training detached on GPU-1 → eval pending
- **Hypothesis (H1):** A LoRA r=32 fine-tune of OpenVLA-7B on LIBERO-Spatial recovers most of the full fine-tuned success rate — target within a few points of the released ~84.7% (REF gate passed at **85.4%**).

- **Dataset / eval set (FROZEN — `research/datasets/libero-spatial-eval.md`):**
  libero_spatial · 10 tasks · 50 trials/task · seeds {7, 42, 123} · `center_crop=True` · `unnorm_key=libero_spatial_no_noops`.
  Training data: `libero_spatial_no_noops` RLDS (~1.8G) → `~/vla/modified_libero_rlds/`.

- **Model / arm:** Arm A — own LoRA, adapter merged into base at bf16 → `~/vla/merged/arm-A-bf16`. Base: `openvla/openvla-7b` (base repo, **not** OFT).

- **Hyperparameters:** lora_rank 32 · lr 5e-4 · effective batch 128 · max_steps 50k (save every 5k; **best checkpoint selected on dev subset**, not necessarily final) · image_aug True · `attn_implementation="sdpa"`.

- **Hardware / env:** RTX PRO 6000 96 GB · **GPU-1 only** (`CUDA_VISIBLE_DEVICES=1`) · torch cu128 (Blackwell/sm_120) · mujoco 3.3.2 · bitsandbytes _____ · W&B offline (remember `wandb sync`).

- **Result (five metrics — fill after eval, via `measure.py` + run_libero_eval):**
  | metric | value |
  |---|---|
  | success rate (%) | _____ (mean over seeds {7,42,123}, ± std) |
  | latency (ms, median) | _____ |
  | peak memory (GB) | _____ |
  | model size (GB) | _____ |
  | throughput (act/s) | _____ |
  | energy (J/action) | _____ (NVML) |

- **What failed / surprised me:**
  - Landmine (fixed): after renaming folder + `dataset_info.name` to `libero_spatial_no_noops`, TFRecord shards must also be renamed `libero_spatial-train.*` → `libero_spatial_no_noops-train.*`, or TFDS raises NotFoundError.
  - Patched `finetune.py` to `attn_implementation="sdpa"` (no flash-attn on Blackwell).
  - _____ (training curve notes, best-checkpoint step, any divergence)

- **Next experiment:**
  1. Eval intermediate adapters on dev subset (10 trials, seed 7) → pick best; **stop early if success plateaus** (~84–85% and flat) to save wall-clock.
  2. Merge best → `~/vla/merged/arm-A-bf16` on GPU-1.
  3. Full frozen eval on best + REF only → this table.
  4. **EXP-002 (int8)** and **EXP-003 (int4)** — same `arm-A-bf16` weights, precision only; reuse `measure.py`. Instrument energy from here on so no re-runs later.
